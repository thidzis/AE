name: blank

on:
  push:

jobs:
  application:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        group: ["grape", "quit", "abuse", "electron", "praise"]
        clay: ["forever", "format", "installment", "tremendous"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Dt
